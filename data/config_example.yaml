# 脚本默认配置，用户不可在前端修改
default:
  logging:
    filename: QBittorrent-Helper.log
    level: INFO

# 用户配置，用户可在前端修改，将会保存到这里
user_config:
  qbittorrent:
    host: http://192.168.0.123:8080
    password: admin
    username: password
  webhook:
    serverchan:
      sc_key: 1234xxxx
user_rules: []
user_tasks: []